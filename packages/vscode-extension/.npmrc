# 继承根目录配置，仅添加扩展打包专属配置
# 禁用 npm 的 production 模式（避免依赖安装不全）
production=false
# 允许 pnpm 从根目录提升依赖
hoist-pattern[]=*

# === pnpm 核心兼容配置 ===
# 强制平铺依赖，生成无符号链接的 node_modules（适配 vsce）
node-linker=hoisted
# 提升所有依赖到根目录，避免 monorepo 子包依赖嵌套
hoist=true
# 禁用符号链接（兜底）
symlink=false
# 明确 pnpm 缓存目录（避免路径问题）
virtual-store-dir=.pnpm-store
